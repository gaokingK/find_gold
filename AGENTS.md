# AGENTS.md

## 当前阶段要求

1. 先讨论需求，确认需求边界和口径。
2. 需求讨论确认后，生成需求文档。
3. 生成接口文档，覆盖接口用途、请求方式、参数、响应结构、错误处理。
4. 最后修改页面显示效果。

## 工作顺序

1. 需求讨论与确认。
2. 编写需求文档。
3. 编写接口文档。
4. 调整页面显示效果。

## 工作原则

- 需求未确认前，不要直接修改页面或代码。
- 文档和界面文案优先使用中文。
- 接口文档必须能支撑前后端联调。
- 每次开始新工作前，先阅读本文件确认当前阶段要求。



## 项目角色与分工

- 本项目的**前端由我负责**，技术栈以我指定的为准。
- **后端由一名新手负责**，使用 **Flask**。
- AI 在本项目中的核心职责是：**编排后端任务、生成接口文档和需求文档**，帮助新手后端按清晰的步骤完成开发。
- AI 不应假设后端人员熟悉 Flask 高级特性、项目分层、部署、异步、数据库迁移等概念。凡是涉及这些内容，必须给出可照做的具体步骤。

## 后端任务编排原则

### 1. 由易到难

后端任务必须按难度递进排列，每个任务只引入一个新概念或一个新依赖。建议顺序：

1. **环境搭建**：安装 Python、创建虚拟环境、安装 Flask、跑通 Hello World。
2. **最小可运行接口**：单个 GET 接口，返回硬编码 JSON。
3. **路由与请求参数**：GET 带 query 参数、POST 带 JSON body。
4. **项目结构拆分**：从单文件拆成 `app/`、`routes/`、`models/` 等。
5. **数据库接入**：使用MySQL；先手写 SQL，再上 ORM。
6. **数据校验**：请求参数校验、错误返回格式统一。
7. **业务逻辑分层**：service 层、repository 层，逐步引入。
8. **认证与权限**：先做简单 token，再做角色权限。
9. **测试**：pytest 写接口测试，先测 happy path。
10. **部署**：gunicorn + nginx，最后做。

每个任务必须写清楚：
- **目标**：这个任务完成后，能跑通什么。
- **前置条件**：依赖哪个任务已完成。
- **具体步骤**：命令、代码位置、关键代码片段。
- **验证方式**：用什么命令、什么请求、期望什么输出。
- **常见坑**：新手容易犯的错误及解决办法。

### 2. 任务粒度

- 单个任务控制在**新手 1–2 小时能完成**的规模。
- 如果一个任务涉及超过 2 个新概念，必须拆开。
- 每个任务都要有**可独立验证的产出**，不能是“写一半”的状态。

## 接口文档要求

每个接口必须包含以下字段，缺一不可：

- **接口名称**：中文 + 英文标识。
- **请求方法与路径**：如 `POST /api/v1/employees/leave`。
- **功能说明**：一句话说清这个接口干什么。
- **请求头**：Content-Type、认证方式等。
- **请求参数**：逐个字段列出，包含：
  - 字段名
  - 类型
  - 是否必填
  - 示例值
  - 说明（含边界条件、格式要求）
- **请求示例**：完整的 curl 或 JSON 示例，可直接复制运行。
- **响应字段**：逐个字段列出，包含类型和说明。
- **成功响应示例**：完整 JSON。
- **错误响应示例**：至少列出 2–3 种常见错误（参数缺失、类型错误、权限不足等），含 HTTP 状态码和错误码。
- **业务规则**：这个接口背后的逻辑约束，如“离职日期不能早于入职日期”。
- **关联接口**：这个接口依赖或影响哪些其他接口。

## 需求文档要求

每个功能模块必须包含：

- **背景与目标**：为什么要做这个功能，解决什么问题。
- **用户角色**：谁会用这个功能。
- **使用场景**：具体在什么情况下用，举 1–2 个例子。
- **功能范围**：明确做什么，**也明确不做什么**。
- **数据字段定义**：涉及的实体、字段、类型、约束。
- **流程说明**：从用户操作到后端处理的完整流程，必要时用文字描述时序。
- **边界与异常**：空数据、重复提交、并发、权限不足等情况的处理方式。
- **验收标准**：怎样算这个功能做完了，可逐条勾选。

## 给新手后端的沟通规范

- 不要用“显然”“众所周知”“很简单”这类词。
- 涉及新概念时，先用一句话解释它是什么、为什么需要它，再给代码。
- 命令必须写完整，包含在哪个目录执行。
- 代码片段必须标注**文件路径**，并说明是新建还是修改。
- 出错时，给出**可能的 2–3 种原因**和对应的排查命令。
- 不要一次抛出多个任务，按编排顺序逐个推进。

## 前端与后端的协作约定

- 接口文档由 AI 维护，放在 `docs/api/` 下，前后端共用同一份。
- 需求文档放在 `docs/requirements/` 下。
- 前端在后端接口未完成前，可基于接口文档使用 mock 数据开发。
- 接口字段一旦确定，变更必须同步更新文档，并通知前端。
- API 路径统一加版本前缀，如 `/api/v1/`。

## 输出格式偏好

- 任务编排用**有序列表**，每个任务带目标、步骤、验证、常见坑。
- 接口文档用**表格 + 示例**，避免大段文字。
- 需求文档用**分节标题**，每节内容简短可扫读。
- 所有代码块标注语言，命令块标注执行目录。
